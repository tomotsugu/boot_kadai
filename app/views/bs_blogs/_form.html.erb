<%= form_with(model: bs_blog, local: true) do |form| %>
  <% if bs_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bs_blog.errors.count, "error") %> prohibited this bs_blog from being saved:</h2>

      <ul>
      <% bs_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :bs_blog_title %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :bs_blog_email %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :bs_blog_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
